{
  "buildCommand": "node -e \"const fs=require('fs');const {execSync}=require('child_process');if(fs.existsSync('frontend')){execSync('cd frontend && npm run build',{stdio:'inherit'})}else{execSync('npm run build',{stdio:'inherit'})}\"",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
